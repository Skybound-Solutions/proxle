// Compact Billboard and Streak Row Components

function BillboardItem({ supporter, rank }: { supporter: Supporter, rank: number }) {
    const medals = ["🏆", "🥇", "🥈"];
    
    return (
        <div className="relative p-2 rounded-lg border border-white/10 bg-white/5 overflow-hidden">
            <div className="flex items-center gap-2">
                <div className="relative flex-shrink-0">
                    {supporter.photoURL ? (
                        <img src={supporter.photoURL} className="w-7 h-7 rounded-full" alt="" />
                    ) : (
                        <div className="w-7 h-7 rounded-full bg-white/10 flex items-center justify-center text-sm">✨</div>
                    )}
                    <div className="absolute -top-1 -left-1 text-sm">{medals[rank - 1]}</div>
                </div>
                <div className="flex-1 min-w-0">
                    <div className="flex items-center gap-1.5 flex-wrap text-xs">
                        <span className="font-bold text-white truncate">{supporter.displayName}</span>
                        {supporter.showStreak && supporter.currentStreak > 0 && (
                            <span className="text-[9px] text-orange-300">🔥{supporter.currentStreak}</span>
                        )}
                        <span className="text-[9px] text-white/40">
                            {supporter.showDonationAmount ? `$${supporter.amount.toFixed(2)}` : '💎'}
                        </span>
                    </div>
                    {supporter.message && supporter.approvalStatus === 'approved' && (
                        <p className="text-[9px] italic text-white/50 truncate">"{supporter.message}"</p>
                    )}
                </div>
            </div>
        </div>
    );
}

function StreakLeaderRow({ leader }: { leader: StreakLeader }) {
    return (
        <div className={clsx(
            "flex items-center gap-2 p-1.5 rounded-lg transition-colors",
            leader.isCurrentUser ? "bg-violet-500/20" : "hover:bg-white/5"
        )}>
            <span className="w-6 text-center text-[10px] font-mono text-white/30">
                {leader.rank === '?' ? '-' : `#${leader.rank}`}
            </span>
            <div className="w-6 h-6 rounded-full bg-white/5 flex items-center justify-center text-[10px] flex-shrink-0">
                {leader.photoURL ? (
                    <img src={leader.photoURL} className="w-full h-full rounded-full" alt="" />
                ) : '✨'}
            </div>
            <span className="flex-1 text-xs text-white truncate">{leader.displayName}</span>
            <span className="flex items-center gap-0.5 text-[10px] text-orange-300 font-bold">
                🔥{leader.currentStreak}
            </span>
        </div>
    );
}
